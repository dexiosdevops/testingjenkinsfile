pipeline {
    agent any
    parameters {
        string(name: 'STATEMENT', defaultValue: 'Class', description: 'What should I say?')
    }
    stages {
        stage('Build') { 
            steps {
                // 
              echo "Hello world"
            }
        }
    }
}
